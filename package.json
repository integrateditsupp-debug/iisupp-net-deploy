{
  "name": "iisupport-net",
  "version": "1.5.0",
  "private": true,
  "description": "Integrated IT Support \u2014 main site + ARIA AI helpdesk",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "kb:build": "node scripts/build-kb-index.mjs",
    "kb:test": "node scripts/test-kb-queries.mjs",
    "dev": "npx -y http-server -p 8765 -c-1 --silent",
    "dev:netlify": "netlify dev"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "@netlify/blobs": "^7.4.0",
    "nodemailer": "^6.9.16"
  }
}